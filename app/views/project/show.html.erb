
<%= form_for :project_item_comment do |f| %>
 Comment on this project piece: <%= f.text_field :content %><br />     Comment on this project piece: <%= f.text_field :content %><br />
+ <%= f.hidden_field :project_item_id, :value => project_item.id%>
+ <%= f.hidden_field :user_id, :value => current_user.id%>
   <%= f.button :submit %>       <%= f.button :submit %>
 <% end %>     <% end %> 

<%= form_for :project_item do |f| %>
  <%= f.fields_for :project_item_comment do |builder| %>
    <%= label_tag :content %>
    <%= builder.text_field :content %>
  <% end %>
  <%= f.submit %>
  <% end %>
