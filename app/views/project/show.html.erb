<h1>Project#show</h1>
<p>Find me in app/views/project/show.html.erb</p>

<%= form_for @project_item_comment, :url => { :controller => "project_item_comment_controller", :action => "create" }, :html => {:method => :post} do |f| %>
<%= f.input :content %>
  <%= f.button :submit %>
<% end %>