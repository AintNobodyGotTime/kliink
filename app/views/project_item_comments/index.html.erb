<p id="notice"><%= notice %></p>

<h1>Listing Project Item Comments</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @project_item_comments.each do |project_item_comment| %>
      <tr>
        <td><%= link_to 'Show', project_item_comment %></td>
        <td><%= link_to 'Edit', edit_project_item_comment_path(project_item_comment) %></td>
        <td><%= link_to 'Destroy', project_item_comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Project item comment', new_project_item_comment_path %>
