<!DOCTYPE html>
<html>
<head>
  <title>Kliink</title>

  <% if current_user != nil %>
  <script>
    var current_user = <%=current_user.id %>;
  </script>
  <% end %>

  <% if @project != nil %>
  <script>
    var project_id = <%=@project.id %>;
  </script>
  <% end %>

  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">

  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>

  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>
<!-- Navigation for all pages -->
    <div class="navbar navbar-default">
        <div class="container-fixed">
            <div class="navbar-header">
               <a href="/"><img src="/assets/kliink.png" style="height:120px;"</a>
            </div>

                  <div class="navbar pull-right" id="nav-buttons">
                    <% if !user_signed_in? %>
                      
                     <%= link_to 'Log in', new_user_session_path, class: 'btn btn-success navbar-btn-lg'; %> 
                    
                    <%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-success navbar-btn-lg' %>
                   
                    <% end  %>
                    
                    <% if user_signed_in? %>
                    <%= link_to 'Sign out', destroy_user_session_path, :method=>'delete', class: 'btn btn-success' %>
                     <% end  %>
                </div>
                </div>
            </div>
<div class="container">
<% if notice %>
  <p class="alert alert-success"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-danger"><%= alert %></p>
<% end %>
</div>

<%= yield %>

</body>
</html>
